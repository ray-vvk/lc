---
name: engineering
description: Designing and validating elements and mechanisms used in products. 
---

{/* here i will import a component that fetches all posts with the tag engineering and renders a bunch of links */}
{/* users should be able to sort the list by various fields. length, date published, date updated, alphabetical, etc */}

{/* actual implementation: nano store that holds sortCategory string. article of tag list will look at this value and based on it display different things. */}

import ArticleTable from "../../components/article/article-table.astro"
import TagLayout from "../../layouts/tag-layout.astro"
import SectionBasic from "../../components/tags/section-basic.astro";
import SectionWithAside from "../../components/article/section-with-aside.astro";
import SectionReferences from "../../components/article/section-references.astro";
import ArticleCallout from "../../components/article/article-callout.astro";
import ArticleOfTagList from "../../components/tags/article-of-tag-list.tsx"
import ArticleOfTagListWrapper from "../../components/tags/article-of-tag-list-wrapper.astro"

<TagLayout>
  <SectionBasic h2={frontmatter.name}>
    <p>{frontmatter.description}</p>
    <ArticleOfTagListWrapper tag={frontmatter.name} />
  </SectionBasic>
</TagLayout>
