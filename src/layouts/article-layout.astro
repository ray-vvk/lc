---
import ArticleHeader from "../components/article/article-header.astro";
import GlobalStyles from "../components/styles/global-styles.astro";
import GlobalLayout from "./global-layout.astro";
interface Props {
  articleTitle: string;
  articleSubtitle: string;
  articleCategory: string;
  articleTags: string[]
  articlePubDate: string; 
  articleUpdateDate: string 
}
const { articleTitle, articleSubtitle, articleCategory, articleTags, articlePubDate, articleUpdateDate } = Astro.props;
---

<GlobalLayout>
  <ArticleHeader
    category={articleCategory}
    subtitle={articleSubtitle}
    title={articleTitle}
    tags={articleTags}
    pubDate={articlePubDate}
    updatedDate={articleUpdateDate}
  />
  <main>
    <slot />
  </main>
</GlobalLayout>

<GlobalStyles />
