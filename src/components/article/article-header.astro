---
interface Props {
  title: HTMLElement
  subtitle: HTMLElement
  category: HTMLElement
}
const { title, subtitle, category } = Astro.props;
---

<header>
  <div class="wrap">
    <div class="eyebrow">{category}</div>
    <h1>{title}</h1>
    <p class="dek">{subtitle}</p>
  </div>
</header>
